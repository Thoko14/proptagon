version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci   # or "npm install" until lockfile is fixed
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

